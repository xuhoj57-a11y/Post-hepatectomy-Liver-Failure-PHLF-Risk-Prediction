# PHLF Predictor English Package

Open `phlf_predictor.html` in a browser to run the tool.

Required model data file:
- `model_rf_integrated.json`

Keep these files in the same folder. The prediction runs locally in the browser and does not upload patient data.
